user.username=${user.username}
user.password=${user.password}

grid2.hub=${grid2.hub}

site.url=http://www.makemytrip.com

browser.name=CHROME
browser.version=${browser.version}
browser.platform=WINDOWS