name: Tests
verbose: 5
dataProviderThreadCount: 1
tests:
  - name: Test1
    verbose: 5
    parameters: { path: / }
    includedGroups: [ System Test ]
    excludedGroups: [ functional ]
    metaGroups: { End to end: [ functional ] , System Test: [ functional,Integration ]  }
    classes:
      - org.tests.pages.HomePageTest

